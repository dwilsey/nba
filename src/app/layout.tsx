import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { AuthProvider } from '@/components/providers/AuthProvider';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'NBA Predictions | Sports Betting Analytics',
  description: 'NBA game predictions, odds comparison, and bet tracking powered by statistical analysis.',
  keywords: ['NBA', 'predictions', 'sports betting', 'odds', 'analytics', 'basketball'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
